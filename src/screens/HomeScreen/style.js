//common styles for HomeScreen
// styles.js
import { StyleSheet } from "react-native";
import { theme } from "../../styles"; // Importing theme from the global styles

export const style = StyleSheet.create({});
